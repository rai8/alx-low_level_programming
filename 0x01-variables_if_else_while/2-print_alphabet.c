#include <stdio.h>
/**
 * main - Alphabet Beginning point
 *
 * Return: 0 for (Success)
 */
int main(void)
{
char abc;

for (abc = 'a'; abc <= 'z'; abc++)
{
putchar(abc);
}
putchar('\n');
return (0);
}
